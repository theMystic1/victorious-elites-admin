import School_Layout from "@/components/ui/layouts/school-layout";

const SchoolLayout = ({ children }: { children: React.ReactNode }) => {
  return <School_Layout>{children}</School_Layout>;
};

export default SchoolLayout;
